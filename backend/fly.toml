app = "pathsix-backend"
primary_region = "dfw"

[env]
  PORT = "8000"

[processes]
  app = "/venv/bin/hypercorn -b 0.0.0.0:8000 asgi:app"

[[services]]
  processes = ["app"]
  internal_port = 8000
  protocol = "tcp"

  [[services.ports]]
    handlers = ["http"]
    port = 80

  [[services.ports]]
    handlers = ["tls", "http"]
    port = 443

[[vm]]
  memory = "1gb"
  cpu_kind = "shared"
  cpus = 1

[autoscaling]
  min_machines_running = 1
  max_machines_running = 1
